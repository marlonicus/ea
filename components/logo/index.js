import styled from "styled-components";

const Logo = styled.div`
  background: transparent url("/static/header-logo.gif") center center / contain
    no-repeat;
  width: 200px;
  height: 40px;
`;

export default Logo;
